[project]
name = "jacket-simulator"
version = "0.1.0"
description = "Desktop simulator for jacket-client LED visualization"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
jacket-simulator = "simulator:run"
